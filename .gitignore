CLAUDE.md
AGENTS.md
.agents/
.gemini/
.claude/
.cursor/
.windsurf/
.continue/
.clinerules/
.roo/
.aider*
skills-lock.json

# === Project binary/doc files (không track) ===
leftover-recipe-matcher-architecture-v3.drawio
so-do-use-case-khach-hang.drawio
leftover-recipe-matcher-cosolythuyet-edited.docx

# báo cáo LaTeX (file cá nhân, không cần track)
bao-cao-latex/

specs/
db-dumps/

# Subagent-driven-development scratch workspace (ledger/briefs/reports, không phải code)
.superpowers/
docs/

# === IDE ===
.idea/
.vscode/
*.iml

# === OS ===
.DS_Store
Thumbs.db

# === Build (Java/Maven) ===
target/
*.class
*.jar
*.war

# === Node (React frontend) ===
node_modules/
dist/
build/

# === Environment / Secrets ===
.env
.env.local
.env.*.local
!.env.example

# Bộ 30 ảnh đánh giá — không commit ảnh (bản quyền/dung lượng), giữ harness
eval/image/images/*
!eval/image/images/.gitkeep

# Báo cáo tổng hợp dùng riêng cho đồ án, không phải tài liệu vận hành hệ thống
eval/results/bao-cao-tong-hop-danh-gia.md

# === Terraform (infra/aws) — state + tfvars thật chứa secret, KHÔNG commit ===
infra/**/.terraform/
infra/**/*.tfstate
infra/**/*.tfstate.*
infra/**/terraform.tfvars
infra/**/*.tfplan
